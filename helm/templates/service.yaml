apiVersion: v1
kind: Service
metadata:
  name: {{ include "youtube-converter.fullname" . }}
  labels:
    {{- include "youtube-converter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "youtube-converter.selectorLabels" . | nindent 4 }}
