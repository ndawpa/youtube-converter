services:
  youtube-converter:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - cookies:/app/data
    environment:
      - COOKIES_PATH=/app/data/cookies.txt
    restart: unless-stopped

volumes:
  cookies:
